{
	"$schema": "https://schema.tauri.app/capability/1",
	"identifier": "default",
	"description": "Default capability for the splash window. The workbench itself is served over loopback HTTP by the VS Code server and does not use Tauri IPC.",
	"windows": ["main"],
	"permissions": ["core:default"]
}
