{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The workbench talks to the backbone over a local HTTP/WS bridge, not Tauri IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
